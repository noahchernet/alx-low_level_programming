#include "main.h"
/**
 * add - adds two numbers, int a and int b
 * @a: the first number to be added
 * @b: the second number to be added
 * Return: @a + @b, the sum of the integers
 */
int add(int a, int b)
{
	return (a + b);
}
